{
  "name": "autopay-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "yarn && cypress open --headed --browser chrome --config-file cypress.json",
    "test": "cypress run --headed --browser chrome --config-file cypress.json",
    "start": "cypress run --headless --config-file cypress.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^4.9.0",
    "mocha": "^8.0.1",
    "mochawesome": "^6.1.1"
  }
}
